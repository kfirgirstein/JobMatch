app.controller('dashboard', function ($scope, $http, $interval, $timeout, $filter, $sce) {

        // common functions 
        app.utils($scope, $timeout);
       
        ////////////////////// start //////////////////////////////
        angular.element(document).ready(function () {

        });


});

               


